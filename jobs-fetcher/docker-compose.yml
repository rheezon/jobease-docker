

services:
  app:
    build:
      context: .
      platforms:
        - linux/amd64
        - linux/arm64
    container_name: job_fetcher
    env_file: .env
    restart: unless-stopped
    command: ["sh", "-c", "ls -la /app && python scheduler.py"]
    network_mode: "host"